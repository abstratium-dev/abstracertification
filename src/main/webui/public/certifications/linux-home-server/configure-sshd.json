{
  "id": "configure-sshd",
  "title": "Configuring the SSH Daemon (sshd)",
  "why": "SSH (Secure Shell) is the primary way you will remotely manage your server. Properly configuring sshd is critical for security. The default configuration works, but hardening it — disabling root login, changing the default port, and using key-based authentication — makes your server significantly more resistant to brute-force attacks and unauthorized access.",
  "instructions": [
    {
      "text": "Verify that OpenSSH server is installed and running.",
      "command": "sudo systemctl status sshd"
    },
    {
      "text": "Open the SSH configuration file for editing.",
      "command": "sudo nano /etc/ssh/sshd_config"
    },
    {
      "text": "Disable root login by finding the 'PermitRootLogin' line and setting it to 'no'.",
      "command": "PermitRootLogin no",
      "note": "This prevents anyone from logging in directly as root via SSH."
    },
    {
      "text": "Optionally, change the default SSH port from 22 to a non-standard port (e.g. 2222) to reduce automated scanning.",
      "command": "Port 2222",
      "note": "If you change the port, remember to update your UFW rules and port forwarding accordingly."
    },
    {
      "text": "Enable key-based authentication (it's usually enabled by default, but verify).",
      "command": "PubkeyAuthentication yes"
    },
    {
      "text": "Generate an SSH key pair on your local machine (not the server) if you don't have one.",
      "command": "ssh-keygen -t ed25519 -C \"your_email@example.com\"",
      "note": "Press Enter to accept the default file location. Set a passphrase for extra security."
    },
    {
      "text": "Copy your public key to the server.",
      "command": "ssh-copy-id -i ~/.ssh/id_ed25519.pub your_user@your_server_ip"
    },
    {
      "text": "Once key-based login works, disable password authentication for maximum security.",
      "command": "PasswordAuthentication no",
      "note": "Only do this after confirming you can log in with your SSH key!"
    },
    {
      "text": "Restart the SSH service to apply changes.",
      "command": "sudo systemctl restart sshd"
    },
    {
      "text": "If you changed the SSH port, update UFW.",
      "command": "sudo ufw allow 2222/tcp\nsudo ufw delete allow OpenSSH"
    }
  ],
  "questions": [
    {
      "id": "q-ssh-1",
      "text": "Why should you disable root login via SSH?",
      "options": [
        "The root account does not exist on Ubuntu",
        "To prevent direct remote access as root, reducing the risk of compromise",
        "Because root cannot use SSH by design",
        "To speed up SSH connections"
      ],
      "correctIndex": 1
    },
    {
      "id": "q-ssh-2",
      "text": "What is the advantage of key-based authentication over passwords?",
      "options": [
        "Keys are shorter and easier to remember",
        "Keys are immune to brute-force attacks and are significantly more secure",
        "Keys don't require any configuration",
        "Passwords are more secure but less convenient"
      ],
      "correctIndex": 1
    },
    {
      "id": "q-ssh-3",
      "text": "What should you do before disabling password authentication?",
      "options": [
        "Disable the firewall",
        "Reboot the server",
        "Confirm that key-based authentication is working",
        "Uninstall OpenSSH and reinstall it"
      ],
      "correctIndex": 2
    }
  ]
}
