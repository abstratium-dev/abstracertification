{
  "id": "port-forwarding",
  "title": "Setting Up Port Forwarding on Your Router",
  "why": "Your home server sits behind your router, which uses NAT (Network Address Translation) to share one public IP address among all devices on your network. By default, incoming connections from the internet cannot reach your server. Port forwarding tells your router to direct traffic on specific ports to your server's internal IP address, making your services accessible from outside your local network.",
  "instructions": [
    {
      "text": "Find your server's local IP address. This is the address you will forward traffic to.",
      "command": "ip addr show | grep 'inet '",
      "note": "Look for the address on your primary network interface (e.g. 192.168.1.x or 10.0.0.x). Ignore the 127.0.0.1 loopback address."
    },
    {
      "text": "Consider assigning a static IP to your server so the address doesn't change. Edit the Netplan configuration:",
      "command": "sudo nano /etc/netplan/01-netcfg.yaml",
      "note": "Set a static address outside your router's DHCP range to avoid conflicts."
    },
    {
      "text": "Apply the Netplan configuration.",
      "command": "sudo netplan apply"
    },
    {
      "text": "Access your router's admin interface. This is usually at 192.168.1.1 or 192.168.0.1 in your web browser. Log in with your router's admin credentials."
    },
    {
      "text": "Navigate to the Port Forwarding section. This is often found under 'Advanced', 'NAT', or 'Firewall' settings depending on your router model."
    },
    {
      "text": "Create port forwarding rules for the services you want to expose. Forward external port 80 (HTTP) to your server's internal IP on port 80, and external port 443 (HTTPS) to port 443."
    },
    {
      "text": "If you changed your SSH port, forward that port as well (e.g. external port 2222 to internal port 2222).",
      "note": "Only forward SSH if you genuinely need remote access from outside your network. It increases your attack surface."
    },
    {
      "text": "Save the router configuration and test from outside your network (e.g., using your mobile phone on cellular data) by navigating to your public IP address.",
      "command": "curl ifconfig.me",
      "note": "Run this on your server to find your public IP address."
    }
  ],
  "questions": [
    {
      "id": "q-port-1",
      "text": "Why is port forwarding necessary to access your server from the internet?",
      "options": [
        "Linux servers cannot connect to the internet without it",
        "Your router uses NAT, so incoming traffic needs to be explicitly directed to your server's internal IP",
        "Port forwarding encrypts the traffic for security",
        "It increases your internet speed"
      ],
      "correctIndex": 1
    },
    {
      "id": "q-port-2",
      "text": "Why should you assign a static IP to your server?",
      "options": [
        "Static IPs are faster than dynamic ones",
        "So the port forwarding rules always point to the correct internal address",
        "DHCP does not work with Linux servers",
        "It is required by Nginx to function"
      ],
      "correctIndex": 1
    },
    {
      "id": "q-port-3",
      "text": "Should you forward your SSH port to the internet?",
      "options": [
        "Always, it is required for the server to work",
        "Never, SSH should be completely disabled",
        "Only if you need remote access from outside your network, as it increases attack surface",
        "Only on port 22, never on a custom port"
      ],
      "correctIndex": 2
    }
  ]
}
